use super::{Z80RegisterName, Z80RegisterPair};

pub enum Z80Instruction {
	// 8-bit loads
	LDBn(u8),
	LDCn(u8),
	LDDn(u8),
	LDEn(u8),
	LDHn(u8),
	LDLn(u8),
	LDAA,
	LDAB,
	LDAC,
	LDAD,
	LDAE,
	LDAH,
	LDAL,
	LDAmHL,
	LDBA,
	LDBB,
	LDBC,
	LDBD,
	LDBE,
	LDBH,
	LDBL,
	LDBmHL,
	LDCA,
	LDCB,
	LDCC,
	LDCD,
	LDCE,
	LDCH,
	LDCL,
	LDCmHL,
	LDDA,
	LDDB,
	LDDC,
	LDDD,
	LDDE,
	LDDH,
	LDDL,
	LDDmHL,
	LDEA,
	LDEB,
	LDEC,
	LDED,
	LDEE,
	LDEH,
	LDEL,
	LDEmHL,
	LDHA,
	LDHB,
	LDHC,
	LDHD,
	LDHE,
	LDHH,
	LDHL,
	LDHmHL,
	LDLA,
	LDLB,
	LDLC,
	LDLD,
	LDLE,
	LDLH,
	LDLL,
	LDLmHL,
	LDmHLB,
	LDmHLC,
	LDmHLD,
	LDmHLE,
	LDmHLH,
	LDmHLL,
	LDmHLn(u8),
	LDAmBC,
	LDAmDE,
	LDAmHL,
	LDAmnn(u16),
	LDmBCA,
	LDmDEA,
	LDmnnA(u16),
	LDAmC, // m = (C + 0xFF00)
	LDmCA, // m = (C + 0xFF00)
	LDDAmHL, // Decrement HL after load
	LDDmHLA, // Decrement HL after load
	LDIAmHL, // Increment HL after load
	LDImHLA, // Increment HL after load
	LDHmnA(u8), // m
}